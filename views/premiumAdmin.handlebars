<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin - Productos Premium</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header>
    <h1>Admin - Productos Premium</h1>
    <nav>
      <ul>
        <li><a href="/">Inicio</a></li>
        <li><a href="/admin">Administrar Productos</a></li>
        <li><a href="/logout">Cerrar Sesión</a></li>
      </ul>
    </nav>
  </header>
  <main>
    <h2>Administrar Productos Premium</h2>
    <ul>
      {{#each products}}
        <li>
          <span>{{this.name}}</span>
          <span>{{this.description}}</span>
          <span>${{this.price}}</span>
          <a href="/admin/product/{{this._id}}/edit">Editar</a>
          <form action="/admin/product/{{this._id}}?_method=DELETE" method="POST">
            <button type="submit">Eliminar</button>
          </form>
        </li>
      {{/each}}
    </ul>
  </main>
  <footer>
    <p>&copy; EcommerceMati</p>
  </footer>
  <script src="/socket.io/socket.io.js"></script>
  <script src="/js/socketScript.js"></script>
</body>
</html>