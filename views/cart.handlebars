<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Carrito</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header>
    <h1>Carrito de Compras</h1>
    <nav>
      <ul>
        <li><a href="/">Inicio</a></li>
        <li><a href="/index">Carrito</a></li>
        <li><a href="/login">Login</a></li>
      </ul>
    </nav>
  </header>
  <main>
    <ul>
    {{#each cart.products}}
      <li>
        <span>{{this.productId.name}}</span>
        <span>Cantidad: {{this.quantity}}</span>
        <form action="/api/carts/{{../cart._id}}/remove/{{this.productId._id}}" method="POST">
          <button type="submit">Eliminar</button>
        </form>
      </li>
    {{/each}}
    </ul>
    <form action="/api/carts" method="POST">
      <input type="hidden" name="productId" value="{{this._id}}">
      <button type="submit">Agregar al Carrito</button>
    </form>
    <p>Total: {{cart.totalPrice}}</p>
  </main>
  <footer>
    <p>&copy; EcommerceMati</p>
  </footer>
  <script src="/socket.io/socket.io.js"></script>
  <script src="/js/socketScript.js"></script>
</body>
</html>